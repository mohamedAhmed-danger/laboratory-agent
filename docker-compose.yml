services:
  web:
    build: .
    container_name: laboratory-agent
    ports:
      - "127.0.0.1:4500:4500"
    volumes:
      - ./instance:/app/instance
    env_file:
      - .env
    restart: always
